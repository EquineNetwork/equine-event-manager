# Equine Event Manager Bug Fix Log

## 2026-05-01 latest batch
- Reworked the frontend stall assignment experience in `public/class-equine-event-manager-shortcodes.php` so customers now get a dedicated `Stall Assignments` toggle row inside `Stall Reservations`, a clear auto-assignment notice, a `Stall Barn` dropdown, and a color-coded stall grid where available stalls show green, reserved stalls show red, and blocked stalls show gray.
- Added live frontend stall-assignment data wiring so the reservation form now receives barn/block, blocked-stall, and occupied-date data, allowing the stall picker to react to selected dates and stall quantity without relying on freeform input or hidden assumptions.
- Moved the frontend `Stall Assignments` chooser below the `Stalls` and `Required Shavings` product rows so it now follows the actual stall booking controls instead of appearing above them.
- Converted the frontend stall-assignment control into a true toggleable row labeled `Stall Assignment`, with the barn chooser and stall grid hidden until the customer turns that row on.
- Updated the frontend `View Stall Map` action so it now renders as a clear clickable link with a download icon and still opens the uploaded stall map in a new browser window from inside the Stall Assignment flow.
- Converted the admin order-side `Stall Assignment` picker from a tall multi-select box into a searchable Select2 token field so admins can start typing stall numbers and see selected stalls as bubbles.
- Enforced the paid stall-count limit on the admin order-side `Stall Assignment` picker so staff cannot select more stall numbers than the order actually purchased, both in the Select2 UI and again during save.
- Added a visible `Stall Assignments` display row inside the order `Stall Reservation` card so assigned stall numbers now appear back as reviewable chips even before staff open the editable picker.
- Reduced the Stall Charts overview `Reservation` metric tile so the event name now reads as a compact summary value instead of an oversized title block next to the numeric metrics.
- Fixed the Stall Charts desktop metrics row width so it now uses a true four-column layout on that page instead of inheriting the shared five-column overview grid and leaving a blank extra column.

## Version 2.0.73 ongoing batches

### 2026-04-27 batch
- Fixed Settings tab controls so the real tab buttons now receive the mobile layout rules, correct active/inactive focus behavior, and better tab-panel accessibility wiring.
- Fixed TEC Migration event selection so it uses plugin-native card styling instead of brittle inline WordPress markup, improving mobile stacking and reducing right-side overflow risk.
- Tightened narrow-screen behavior for report filters, report history rows, and admin pagination/action areas to better preserve the intended mobile-first app feel.
- Added keyboard navigation support for Settings tabs so left/right/up/down, Home, and End work consistently without breaking the current save flow.
- Fixed the Settings tab/button visual regression so the active pill and Save Settings submit control keep the intended dark primary state instead of falling back to muted text/button styles.
- Fixed disabled Native Events menu cleanup by making the feature default resolve consistently as off when not enabled and by force-pruning any lingering Event/Location/Producer submenu entries under Event Manager.
- Reverted the broad Native Events default change after it caused an orders visibility regression; the safer fix keeps the stronger submenu cleanup logic while preserving the original feature defaults for existing data flows.
- Hardened the Reservations admin registration path by attaching the `en_reservation` post type directly under the Event Manager parent menu instead of relying on a manually-added submenu entry, reducing the chance of list-screen regressions and missing reservation counts.
- Hardened the Orders repository with compatibility fallback table discovery so the admin can still find historical order rows even if the live site is using legacy or alternate reservation table names instead of only the canonical `en_stall_reservations` and `en_rv_reservations` pair.
- Restored the Reservations CPT to the legacy `show_in_menu => true` behavior used in earlier working builds and added empty-state admin diagnostics so the live site now reports detected reservation-post and order-table counts directly on empty Reservations and Orders screens.
- Normalized the Event Manager submenu order so the auto-inserted Reservations CPT entry no longer pushes Dashboard below Reservations.
- Restored a visible `Add Reservation` action on the Reservations list screen by replacing the hidden native page-title action with explicit custom banner buttons.
- Added TEC collision safeguards so native Equine Event Manager events no longer claim the `/events` frontend slug when The Events Calendar is active, default new installs to Native Events off with External as the default event source, and surface clear Settings guidance for TEC-heavy live sites.
- Synced the Event Manager admin menu to the Native Events toggle so event-related submenu items now disappear immediately on the Settings screen when that feature is turned off, matching the saved backend cleanup behavior.
- Fixed the broken Settings save action by removing the nested TEC import form from inside the main Settings form and converting the import button to use a direct `formaction`, restoring normal `Save Settings` submission.
- Kept `Add Reservation` available from the dashboard and reservation list banner only, and removed the dedicated Event Manager submenu item so the left nav stays cleaner.
- Restored the missing `Reservations` submenu entry under Event Manager so the reservation list view appears alongside Dashboard and Add Reservation instead of only existing as a separate top-level WordPress menu item.
- Hardened the Event Manager submenu builder with a fallback reservation entry injection so `Reservations` cannot disappear from the custom submenu even if WordPress drops that CPT-linked slug during menu registration.
- Removed the standalone WordPress `Reservations` CPT menu by setting the reservation post type to `show_in_menu => false` and keeping reservation add/edit/list screens highlighted under the `Event Manager > Reservations` submenu path instead.
- Fixed the Reservations list mobile card layout by wrapping reservation type badges in the expected container and adding a final screen-specific mobile override so `Type`, `Actions`, and `Date` stack cleanly and the `Edit` / `View` buttons render as full-width touch targets inside the card instead of cramped inline buttons.
- Tightened the mobile admin experience across the Reservations Dashboard, Reservation editor, Order details, Refund details, and Invoicing screens by forcing stacked full-width action buttons, removing right-edge overflow, converting dashboard/order summary blocks into cleaner phone-sized cards, and fixing cramped add-on table / payment breakdown / embedded invoice-form controls that were still leaking desktop layout behavior on narrow screens.
- Added a final Reservations-list-only mobile override so the search/filter controls keep consistent spacing, type badges stop stretching awkwardly, and the `Edit` / `View` actions stack as true full-width buttons on phones instead of tiny side-by-side pills.
- Added a final Orders-list mobile override so the filter controls stay consistently stacked, order cards collapse into a tighter single-column rhythm, status/total/date rows stop borrowing desktop column spacing, and the print action sits as a clean touch target instead of floating in an oversized action area.
- Added a final Order-details mobile override so reservation detail grids, refund detail rows, and payment breakdown cards stop compressing into narrow multi-column tiles on phones and instead stack into readable full-width blocks with safer text wrapping.
- Added a shared mobile gutter and width-consistency pass across admin/plugin screens plus the embedded reservation/invoice form so cards, forms, and fields now sit inside the same left/right gray padding and the Reservation Editor postboxes, structured tables, and inputs stop pushing against the viewport on phones.
- Added a final ultra-mobile hardening layer for sub-480px widths so the smallest phones now get stronger gutter enforcement, tighter card padding, full-width fields/buttons, safer text wrapping, and more aggressive Reservation Editor control normalization instead of inheriting any leftover tablet/desktop spacing.
- Removed the temporary legacy bootstrap shim and restored `equine-event-manager.php` as the sole plugin entrypoint after confirming this plugin only exists on a single test site and does not need old-install basename compatibility.
- Replaced the fragile General Add-Ons script-template row with a hidden template-based row generator so raw placeholder markup can no longer leak into the Reservation Editor, and restored a proper large-screen Reservation Editor content/sidebar split so the shortcode and publish cards no longer collapse into a narrow floating strip.
- Adjusted the Reservation Editor desktop structure again so the utility boxes now sit as a deliberate two-card row at the top when they are not in a right sidebar, with the main editor content flowing full width underneath.
- Normalized the Dashboard `Quick Actions` card spacing so its internal padding and header/body rhythm now match the surrounding dashboard cards instead of feeling tighter and visually misaligned.
- Removed the `Settings` shortcut from the Dashboard `Quick Actions` card so that action strip stays focused on reservation and invoicing workflows.
- Fixed the Reservation Editor RV toggle rows so labels like `Enable RV lot selection` and `Enable RV add-ons` are vertically centered against their switch controls instead of sitting low beside the track.
- Rebalanced the desktop Orders table column widths so `Order`, `Customer`, and `Event` no longer consume too much space and the `Status`, `Date`, and `Actions` columns have enough room to stay readable and aligned.
- Added a strict sub-640px RV lot table override so the Reservation Editor lot panel, lot fields, currency inputs, and `Add RV Lot` button all stay inside the mobile gutter instead of spilling off the right edge.
- Added manual `Mark Cash` and `Mark Check` admin actions for unpaid/invoice-sent orders so staff can record in-person payments directly from the Order details screen with the actual payment method stored in the order notes.
- Added the first Stall Chart system pass: a reservation-level `Enable Stall Chart` toggle, numbered stall/RV block range builders in the Reservation Editor, a dedicated `Stall Chart` admin overview screen with date-by-date assignment counts, and editable order-level stall/RV assignment override fields that save back into order component notes.
- Bumped the plugin release to `2.0.75` to package the latest admin UX, manual payment, and first-pass Stall Chart changes together.
- Fixed the Reservation Editor top utility row so the `Reservation Shortcode` and `Publish` cards stretch as an even desktop two-column layout instead of collapsing into uneven stacked/narrow boxes.
- Moved the Reservation Editor `Stall Chart` controls inside the `Stall Reservations` section and removed the broken standalone section behavior so the nested Stall Chart toggle now controls its own block-builder panel correctly.
- Expanded the Stall Chart flow with reservation-side blocked stall/RV unit controls, checkout-time automatic unit assignment persistence into order notes, and a true unit-by-date Stall Chart admin grid that shows customer names inside numbered stall/RV cells instead of only order-level assignment counts.
- Tightened the Order Details `Reservation Details` layout by reducing the per-row tile density on order/refund screens and moving the editable stall/RV assignment fields into that Reservation Details area instead of leaving them in a separate lower card.
- Replaced the order-side free-text assignment inputs with real multi-select Stall/RV assignment pickers populated from the reservation’s configured Stall Chart units, keeping `Stall Assignment` on the Stall Reservation card and preventing admin from having to type unit numbers manually.
- Reworked the Order Details page structure so `Event & Contact` now lives in one full-width summary card, while `Stall Reservation`, `RV Reservation`, `Add-Ons`, and `Group Reservations` render as their own separate cards instead of being bundled under a single Reservation Details section.
- Added a conditional `Stall Chart` action to the Reservations list screen so any reservation with Stall Chart enabled now gets a direct button beside `Edit` and `View`, with the action-button layout adjusted to stay usable when a third action is present.
- Refined the Stall Chart reservation builder so stall/RV ranges now read more clearly as `Title`, `Start #`, and `End #`, and replaced the old blocked-unit multi-select with a dedicated blocked-numbers entry area that accepts comma-separated values and ranges like `7000, 7002-7004`.
- Upgraded the Stall Chart overview screen with a searchable toolbar, direct print action, mobile-stacked chart rows, and print-specific cleanup so staff can search by unit/customer name, review it comfortably on phones, and produce a cleaner paper chart.
- Hardened the Stall Chart reservation builder so overlapping stall or RV ranges can no longer be saved; the editor now shows clearer non-overlap guidance and raises a specific validation error identifying the conflicting ranges.
- Replaced the free-entry blocked-numbers field with a controlled searchable multi-select powered by the generated unit list, so admins can type to find valid stall/RV numbers without risking arbitrary manual entry errors.
- Added live Stall Chart builder guidance so stall/RV ranges now regenerate blocked-number choices immediately, show generated-unit counts, highlight incomplete or overlapping rows before save, and keep the blocked-number picker synced to only valid currently-defined units.
- Renamed the reservation-side `Stall Chart` setup to `Stall Assignments`, kept it inside the `Stall Reservations` card, removed the separate RV block builder, and narrowed the builder inputs so the range editor fits the card more cleanly.
- Reworked the assignments overview to behave more like a hotel-style occupancy board: stalls stay numbered by block, RVs now use configured `RV Lots` instead of made-up numbered RV units, search/print wording now reflects that split, and the RV board renders as lot-by-day occupancy instead of a redundant unit table.
- Updated assignment state colors and labels so availability now reads more clearly at a glance: available cells show green, occupied cells show blue, and blocked cells show red across the assignments board and print/mobile layouts.
- Aligned the order-side assignment controls with the reservation setup by saving `Assigned RV Lots` instead of numbered RV units, falling back to older saved notes when needed, and keeping admin selection tied to configured stall numbers and RV lot names instead of free-entry values.
- Built out the RV side to mirror stall occupancy behavior without copying the stall setup form: RVs still use the existing priced `RV Lots` builder, but now also support searchable blocked-lot selection, blocked/available/occupied day coloring on the assignments board, and lot-based admin assignment controls that match the live reservation setup.
- Hardened the mobile `Stall Assignments` builder so the visible stall-block panel is now the one driving the live summary/count logic, added a correct server-rendered initial range/unit summary, and added legacy UI cleanup to strip any obsolete RV-block controls if they still appear on the reservation editor.
- Added a stricter reservation-editor cleanup pass that force-removes any lingering legacy `RV Blocks`, `Add RV Block`, and `Blocked RV Numbers` markup globally if an older fragment still appears, so the stall assignments area cannot show the obsolete RV-number block builder anymore.
- Fixed the Reservation Editor desktop top utility row so the `Reservation Shortcode` and `Publish` cards now force the full editor width and split into a true 50/50 two-card row instead of shrinking into narrow left-aligned columns that do not match the content width below.
- Loosened the Order Details assignment gating so `Stall Assignment` and `RV Lot Assignment` now render whenever the linked reservation already has configured stall numbers or RV lot names, instead of disappearing entirely just because the toggle state alone resolved false.
- Fixed the `Stall Assignments` screen load path so opening it from the left admin menu no longer dead-ends with a load error when no `reservation_id` is present; it now opens a reservation picker/launcher screen that lists reservations with stall-assignment data and lets admin jump straight into the correct board.
- Removed the odd blue frontend submission outline by clearing the focus ring on the reservation form wrapper itself, while keeping normal focus styling on actual form fields and controls.
- Removed the `Additional Shavings Quantity` tile from the Order Details `Stall Reservation` card so that section stays focused on the core stall stay details and assignment workflow.
- Rebuilt the Order Details `Stall Reservation` card into an intentional two-column layout order: `Stay Type + Nights`, `Arrival + Departure`, `Stall Quantity + Required Shavings`, and a full-width `Stall Assignment` tile with the available-stall selector and save action.
- Renamed `Event & Contact` to `Customer Details`, converted the order details page to a stacked full-width card flow, and reorganized the `Customer Details` summary into four intentional two-up rows so it no longer competes side-by-side with the `Stall Reservation` card.
- Added left padding to the `Title` header in the `Stall Blocks` builder so the first column label aligns with the field content instead of touching the table border.
- Removed the extra RV-occupancy explanatory sentence from the `Stall Assignments` card so the stall setup stays cleaner and more focused.
- Increased the spacing under the `RV Lots` heading so the title no longer feels pressed against the table below it.
- Added true per-lot RV inventory: each `RV Lot` row now has its own inventory field, the selected lot now controls RV availability/quantity on the frontend, sold-out lots are disabled in the lot picker, and checkout validation now respects the remaining count for the chosen lot instead of only the old global RV inventory field.
- Removed the conflicting global `Available RV Inventory` control whenever `RV Lots` is enabled so RV availability has one clear source of truth, and fixed the stacked/mobile RV Lots table labels so the new `Inventory` column is shown correctly instead of inheriting the old `Action` label.
- Added a `Resend Customer Notification` action on the order page so admins can manually resend the customer receipt/order email from any order with an email address on file, and logged the resend timestamp back into the order notes.
- Added SendGrid email-delivery support in `Settings > Integrations` with a saved API key field, and routed both customer receipt emails and payment-link invoice emails through SendGrid automatically when that key is present, with the existing WordPress mailer kept as the fallback.
- Added a backend `Stall Map` upload area directly inside `Stall Assignments`, so reservation staff can attach a customer-facing stall map file without leaving the Stall Reservations card.
- Added a frontend `View Stall Map` link plus grouped preferred-stall selection tiles when Stall Assignments are enabled, letting customers open the map in a new tab and choose valid generated stall numbers without typing them manually.
- Kept automatic stall assignment in place when the customer does not choose a stall, and hardened the auto-assignment persistence so partial customer selections are now filled and saved correctly instead of being ignored once any manual stall value already existed.
- Added a `Generate Stall Assignments` action to Reservation Overview and wired a full admin handler so staff can regenerate stall and RV lot assignments across a reservation in one step using the live assignment rules.
- Brought the `Stall Blocks` builder table onto the same strict reservation-editor layout system as the other structured tables, with matching desktop overflow behavior, full-width mobile collapse rules, and consistent add-button/action alignment at the shared breakpoints.
- Added a stricter Stall Assignments editor cleanup pass so the stale RV-occupancy helper sentence is force-removed too if any older fragment still injects it into the reservation editor.
- Moved the `RV Lots` pricing/inventory helper copy so it now sits directly under the `RV Lots` heading and above the lot table instead of being stranded below the add button.
- Moved the `RV Add-Ons` pricing helper copy so it now appears directly under the `Enable RV add-ons` intro and above the add-ons table instead of being stranded below the add button.
- Fixed the `Enable RV add-ons` toggle row so the label stays vertically centered to the right of the switch instead of dropping underneath it.
- Moved the `General Add-Ons` helper copy so it now sits under the section title and above the add-ons table instead of appearing below the add button.
- Moved the `Stall Blocks` example copy so it now sits directly under the `Stall Blocks` title and above the block table instead of appearing below the add button.
- Moved the `Stall Blocks` live summary/help text onto the same action row as the `Add Stall Block` button on larger screens, while keeping it stacked again on mobile to preserve the strict responsive layout.
- Added a third Reservation Editor utility card for `Stall Assignments` showing assigned stall count, unassigned stall count, and a direct `View Stall Chart` link, and widened the desktop utility-card row to three columns on large screens so it reads as a true top-row status strip.
- Added a `View Stall Charts` button to the Reservations screen header that opens the Stall Assignments chooser screen, so admin can pick which event/reservation chart to view without first opening a specific reservation.
- Renamed the Event Manager submenu item from `Stall Assignments` to `Stall Charts` and moved it directly below `Reservations` in the left admin menu.
- Final mobile hardening pass: widened the new Stall Assignments status-card metric stacking to the standard phone breakpoint and tightened its button sizing/wrapping so the newest reservation-editor utility card behaves consistently with the rest of the mobile-first admin UI.
- Reworked the Reservation Editor utility row so it now orders as `Stall Assignments`, `Reservation Shortcode`, then `Publish`, uses an auto-fit desktop grid that becomes either a three-card or two-card row automatically, and hides the Stall Assignments card entirely when Stall Assignments are not enabled.
- Tightened the `Stall Blocks` table gutters again so the `Title` header and first-column fields now have a matching left inset, the action column has a real right-side gutter, and the cell padding aligns more closely with the other reservation-editor structured tables.
- Fixed a reservation-editor typography scope leak so the WordPress media modal `Select Image` title no longer inherits the oversized reservation page heading styles when uploading a stall map.
- Added a `Daily Movement` summary band to the Stall Charts page so staff can see total arriving and total departing counts for each chart date above the occupancy board before scanning the full stall/RV grid.
- Added a direct `Generate Assignments` action to the Stall Charts page itself and updated the generator redirect flow so running it from that screen returns staff to the same chart with a success/error notice instead of kicking them away.
- Hardened RV auto-assignment so system-generated RV lot assignments only use base lots without add-on pricing, while still preserving any explicit premium-lot choice already made by the customer or admin.
- Added a `Select Barn` dropdown to the Stall Charts toolbar so staff can filter the assignments board by stall block/barn while still using the live search and print tools on the same screen.
- Added a second Stall Charts view, `Customer Night Counts`, so staff can switch from the unit-by-date board to a customer-by-night table showing how many spaces each order occupies on each date, along with the assigned stall numbers and RV lots.
- Replaced the separate Stall Charts view buttons with a simpler dropdown view switcher so staff can treat it like one chart with two display modes: `Stall Assignments View` and `Stall Counts Per Night`.
- Fixed the frontend `Stall Assignment` toggle so it now truly hides and shows the full stall-assignment panel instead of leaving the section visibly present in a disabled-looking state when the toggle is off.
- Hardened the frontend `Stall Barn` filter so only stalls from the currently selected barn are shown, with both the barn group wrappers and the individual stall bubbles now obeying the selected dropdown value.
- Updated the frontend `Stall Assignment` notice copy so it clearly explains that the selector is not a map of facility stall placement and directs customers to the uploaded stall map for actual placement reference.
- Hardened the frontend `View Stall Map` action so it explicitly opens the uploaded stall map in a new browser window for printing or saving instead of relying only on the anchor target behavior.
- Fixed a `Generate Assignments` fatal error by restoring the missing repository note-line removal helper used when assignment generation updates saved RV lot notes, so Stall Charts generation no longer crashes on live runs.
- Removed the `Unassigned` column from the `Customer Night Counts` Stall Charts view so the table stays tighter and focuses only on the nightly counts plus saved stall and RV assignments.
- Refined the frontend admin-invoice `Invoice Delivery` control so the toggle label now reads `Charge Customer` and sits cleanly to the right of the switch instead of dropping underneath it.
- Added a manual `Add to Show Bill` invoice action, surfaced `Outstanding Show Bill` as an order status badge, and added an Orders filter for those flagged show-bill orders so staff can isolate them quickly.
- Fixed the Orders screen route handling by redirecting legacy `post_type=en_reservation&page=equine-event-manager-orders` requests back onto the proper `admin.php?page=equine-event-manager-orders` route, and normalized Orders links/forms/sort/pagination to keep the screen loading reliably while preserving active filters.
- Tightened RV assignment issue detection and generation so Stall Charts only flags/orders auto-assigns RV lots when an order actually has meaningful RV reservation data, preventing false unassigned RV warnings from stray or legacy empty RV-side rows.
- Added a `Generate Assignments` button directly to the Reservation Editor `Stall Assignments` card so staff can generate assignments and jump to the chart from the same status panel.
- Simplified the show-bill status wording to `Show Bill`, updated the Orders filter option to `Show Bills`, and gave the Show Bill badge a brighter high-contrast highlight so staff can spot those orders quickly.
- Recolored the frontend `Event Reservations` eyebrow badge to a neutral gray pill so it fits the rest of the reservation-card palette better and no longer reads as orange.
- Restyled the frontend `Stay Details` section as a subtle instruction card so it stands out more clearly as guidance before the reservation sections below it.
- Added a `Show Bills` dashboard summary card with the count of outstanding show-bill orders and linked it directly to the Orders screen filtered to those flagged bills.
- Added a `Stall Assignments` stat to the dashboard `Booking Snapshot` card and normalized that stat row to an even four-column layout.
